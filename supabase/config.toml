# Supabase Configuration for Lake Ride Pros

[api]
enabled = true
port = 54321
schemas = ["public", "graphql_public"]
extra_search_path = ["public", "extensions"]
max_rows = 1000

[db]
port = 54322
shadow_port = 54320
major_version = 15

[studio]
enabled = true
port = 54323

[edge_functions]
enabled = true

# Note: Edge Function uses manual authentication (checks for valid Supabase anon/service key)
# Secrets should be set via Supabase CLI or Dashboard:
# - OUTSCRAPER_API_KEY (your Outscraper API key)
# - GOOGLE_PLACE_ID (ChIJJ8GI2fuCGWIRW8RfPECoxN4)
# - SUPABASE_ANON_KEY (your Supabase anon key for auth validation)
# - SUPABASE_SERVICE_ROLE_KEY (your Supabase service role key for auth validation)
